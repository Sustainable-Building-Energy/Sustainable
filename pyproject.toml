[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
#where = ["."]
include = ["suse"]
#namespaces = false


[project]
name = "suse"
version = "0.0.1"
authors = [
  {name = "SUSE Authors"}
]
